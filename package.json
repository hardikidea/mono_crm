{
  "name": "mono_crm",
  "version": "1.0.0",
  "description": "Mono Repository Client & Server Configurations Setups",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "npm run start:dev --workspace=server & npm run start:dev --workspace=client",
    "start:dev-server": "cd packages/server && npm run start:dev",
    "start:dev-client": "cd packages/client && npm run start:dev",
    "build": "npm run build --workspace=server && npm run build --workspace=client"
  },
  "author": "Hardik Chauhan",
  "license": "ISC"
}
